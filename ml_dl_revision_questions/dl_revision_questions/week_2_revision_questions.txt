# 📚 WEEK 2 — BACKPROPAGATION, OPTIMIZATION & REGULARIZATION
## All Exercises with Answers in Spoiler Format

---

# SECTION 1: WARM-UP

---

## Exercise 1: "Backpropagation Lifecycle" Flowchart

Place the following steps in the correct order:

| Step | Description | Order (1-4) |
|------|-------------|-------------|
| A | Compute gradients using the chain rule (Backward Pass) | |
| B | Update weights using Gradient Descent | |
| C | Compute the loss value | |
| D | Forward pass: compute activations through the network | |

<details>
<summary>📖 Click for Answers</summary>

| Step | Description | Order |
|------|-------------|-------|
| D | Forward pass: compute activations through the network | **1** |
| C | Compute the loss value | **2** |
| A | Compute gradients using the chain rule (Backward Pass) | **3** |
| B | Update weights using Gradient Descent | **4** |

</details>

---

## Exercise 2: Optimizer Matching

Match each optimizer to its **key mechanism** and **characteristic**:

| Optimizer | Key Mechanism | Characteristic |
|-----------|---------------|----------------|
| SGD | | |
| SGD with Momentum | | |
| RMSProp | | |
| Adam | | |

<details>
<summary>📖 Click for Answers</summary>

| Optimizer | Key Mechanism | Characteristic |
|-----------|---------------|----------------|
| **SGD** | Uses raw gradient | Simple, requires careful LR tuning |
| **SGD with Momentum** | Moving average of gradients | Dampens oscillations, accelerates in consistent directions |
| **RMSProp** | Moving average of squared gradients | Adaptive per-parameter learning rates |
| **Adam** | Moving average of gradients + squared gradients | Combines momentum and adaptive scaling |

</details>

---

## Exercise 3: Regularization Technique Matching

Match each technique to its **primary mechanism**:

| Technique | Primary Mechanism |
|-----------|-------------------|
| Dropout | |
| L2 Regularization (Weight Decay) | |
| Early Stopping | |
| Batch Normalization | |
| Data Augmentation | |

<details>
<summary>📖 Click for Answers</summary>

| Technique | Primary Mechanism |
|-----------|-------------------|
| **Dropout** | Randomly sets neurons to 0 during training; prevents co-adaptation |
| **L2 Regularization** | Adds penalty to large weights; favors simpler models |
| **Early Stopping** | Halts training when validation loss stops improving |
| **Batch Normalization** | Normalizes layer inputs; adds noise and stabilizes training |
| **Data Augmentation** | Creates synthetic variations of training data |

</details>

---

# SECTION 2: CORE TOPICS — BACKPROPAGATION

---

## Exercise 4: "The Chain Rule in Action"

**Scenario:** You have a simple network: `z = w · x + b`, `a = σ(z)`, `L = (a - y)²` (MSE).

| Question | Your Answer |
|----------|-------------|
| 1. Write the chain rule expression for `∂L/∂w` | |
| 2. Write the chain rule expression for `∂L/∂b` | |
| 3. If `x=2`, `y=0`, `w=0.5`, `b=0.1`, and `σ` is Sigmoid, what is `∂L/∂w`? (Show work) | |
| 4. Why do we store intermediate values during the forward pass? | |
| 5. What is the computational complexity of backpropagation in terms of the number of weights (`N`)? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. ∂L/∂w | `∂L/∂w = ∂L/∂a · ∂a/∂z · ∂z/∂w = 2(a-y) · σ'(z) · x` |
| 2. ∂L/∂b | `∂L/∂b = ∂L/∂a · ∂a/∂z · ∂z/∂b = 2(a-y) · σ'(z) · 1` |
| 3. Calculate ∂L/∂w | `z = 0.5×2 + 0.1 = 1.1`; `a = sigmoid(1.1) ≈ 0.75`; `∂L/∂a = 2(0.75-0) = 1.5`; `σ'(z) = a(1-a) = 0.75×0.25 = 0.1875`; `∂z/∂w = x = 2`; **∂L/∂w = 1.5 × 0.1875 × 2 = 0.5625** |
| 4. Why store intermediate values? | Storing activations (`a`) and weighted sums (`z`) during the forward pass prevents recomputation during the backward pass, reducing complexity from exponential to **O(N)** . |
| 5. Computational complexity | **O(N)** — linear with respect to the number of weights. |

</details>

---

## Exercise 5: "Computational Graphs — Local Gradients"

For each primitive operation, write the **local gradient**:

| Operation | Forward: `z = f(x,y)` | `∂L/∂x` | `∂L/∂y` |
|-----------|----------------------|---------|---------|
| Addition | `z = x + y` | | |
| Multiplication | `z = x · y` | | |
| Max | `z = max(x, y)` | | |
| Sigmoid | `z = σ(x) = 1/(1+e^(-x))` | | |
| ReLU | `z = max(0, x)` | | |

<details>
<summary>📖 Click for Answers</summary>

| Operation | Forward: `z = f(x,y)` | `∂L/∂x` | `∂L/∂y` |
|-----------|----------------------|---------|---------|
| Addition | `z = x + y` | `∂L/∂z · 1` | `∂L/∂z · 1` |
| Multiplication | `z = x · y` | `∂L/∂z · y` | `∂L/∂z · x` |
| Max | `z = max(x, y)` | `∂L/∂z` if x > y, else 0 | `∂L/∂z` if y > x, else 0 |
| Sigmoid | `z = σ(x)` | `∂L/∂z · σ(x)·(1-σ(x))` | N/A |
| ReLU | `z = max(0, x)` | `∂L/∂z` if x > 0, else 0 | N/A |

</details>

---

## Exercise 6: "Branching Paths — Gradient Aggregation"

**Scenario:** A variable `x` feeds into two separate operations: `f(x) = 2x` and `g(x) = x²`. The loss is `L = f(x) + g(x)`.

| Question | Your Answer |
|----------|-------------|
| 1. Draw the computational graph (describe branches). | |
| 2. What is `∂L/∂x`? | |
| 3. In a neural network, when do branching paths occur? | |
| 4. In ResNet, if `y = F(x) + x` (skip connection), what is `∂L/∂x`? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. Draw the computational graph | `x` branches into two paths: (1) `f(x)=2x` → `L`, and (2) `g(x)=x²` → `L`. The total gradient is the sum of gradients from both branches. |
| 2. ∂L/∂x | `∂L/∂x = ∂f/∂x + ∂g/∂x = 2 + 2x` |
| 3. When do branching paths occur? | Branching occurs when a tensor is used in multiple operations: **Residual connections**, **Multi-Head Attention** (Q, K, V projections), and **shared weights** in RNNs. |
| 4. ∂L/∂x for ResNet | `∂L/∂x = ∂L/∂y · (∂F/∂x + 1)` — the gradient flows through **both** the transformation path and the shortcut path, mitigating vanishing gradients. |

</details>

---

## Exercise 7: "Jacobian Matrix Basics"

| Question | Your Answer |
|----------|-------------|
| 1. What is the Jacobian matrix? | |
| 2. For a vector function `f: ℝⁿ → ℝᵐ`, what is the shape of the Jacobian? | |
| 3. In backpropagation, why do we multiply Jacobians? | |
| 4. What is the Jacobian of `z = W · a + b` with respect to `a`? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is the Jacobian matrix? | A matrix of all first-order partial derivatives of a vector-valued function. It generalizes the derivative to vector functions. |
| 2. Shape of the Jacobian | **m × n** — rows = outputs (m), columns = inputs (n). |
| 3. Why multiply Jacobians? | Backpropagation is the **product of Jacobians** via the chain rule: `∂L/∂x = (∂z/∂x)ᵀ · ∂L/∂z`. Each layer's Jacobian is multiplied sequentially. |
| 4. Jacobian of `z = W·a + b` w.r.t `a` | **W** (the weight matrix). |

</details>

---

## Exercise 8: "Automatic Differentiation (AD) Modes"

| Question | Your Answer |
|----------|-------------|
| 1. What is Forward Mode AD? When is it efficient? | |
| 2. What is Reverse Mode AD? When is it efficient? | |
| 3. Which mode does backpropagation use? | |
| 4. What is the difference between tape-based (PyTorch) and static graph (TensorFlow 1.x)? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is Forward Mode AD? | Propagates derivatives forward alongside the forward pass. Efficient when **number of inputs is small** (Jacobian-vector product). |
| 2. What is Reverse Mode AD? | Propagates derivatives backward after a forward pass. Efficient for **scalar output functions** (like loss) with **many inputs** (weights). |
| 3. Which mode does backpropagation use? | **Reverse Mode AD** — this is exactly what backpropagation is. |
| 4. Tape-based vs Static Graph | **Tape-based (PyTorch)**: builds graph on-the-fly (dynamic). **Static Graph (TensorFlow 1.x)**: pre-defines and optimizes the graph before execution (faster but less flexible). |

</details>

---

# SECTION 3: CORE TOPICS — OPTIMIZATION

---

## Exercise 9: "Update Rules Comparison"

Complete the table for each optimizer's update rules:

| Optimizer | Update Rule(s) | Key Hyperparameter(s) |
|-----------|---------------|----------------------|
| Standard SGD | | |
| SGD + Momentum | | |
| RMSProp | | |
| Adam | | |

<details>
<summary>📖 Click for Answers</summary>

| Optimizer | Update Rule(s) | Key Hyperparameter(s) |
|-----------|---------------|----------------------|
| **Standard SGD** | `w ← w - η · ∂L/∂w` | Learning rate `η` |
| **SGD + Momentum** | `v ← γv + η·∂L/∂w`<br>`w ← w - v` | Learning rate `η`, Momentum `γ` (≈0.9) |
| **RMSProp** | `E[g²] ← βE[g²] + (1-β)g²`<br>`w ← w - η·g / √(E[g²] + ε)` | LR `η`, Decay `β` (≈0.9), `ε` (≈1e-8) |
| **Adam** | `m ← β₁m + (1-β₁)g`<br>`v ← β₂v + (1-β₂)g²`<br>`m̂ ← m/(1-β₁ᵗ)`<br>`v̂ ← v/(1-β₂ᵗ)`<br>`w ← w - η·m̂/(√v̂ + ε)` | LR `η`, `β₁` (≈0.9), `β₂` (≈0.999), `ε` (≈1e-8) |

</details>

---

## Exercise 10: "Momentum vs RMSProp vs Adam"

| Question | Your Answer |
|----------|-------------|
| 1. What problem does Momentum solve in standard SGD? | |
| 2. What problem does RMSProp solve? | |
| 3. What does Adam combine from both? | |
| 4. Why does Adam require bias correction early in training? | |
| 5. Which optimizer is the current default for most deep learning tasks? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What problem does Momentum solve? | Standard SGD oscillates in steep directions and makes slow progress in shallow directions. Momentum accumulates past gradients to dampen oscillations and accelerate in consistent directions. |
| 2. What problem does RMSProp solve? | RMSProp normalizes gradients by a running average of their magnitudes — adaptive per-parameter learning rates. Makes large updates for infrequent gradients, dampens updates for large gradients. |
| 3. What does Adam combine? | **Momentum** (moving average of gradients) + **RMSProp** (moving average of squared gradients). |
| 4. Why does Adam require bias correction? | At the start of training, momentum (`m`) and velocity (`v`) estimates are initialized at 0, causing biased estimates. Bias correction divides by `(1 - βᵗ)` to compensate. |
| 5. Current default optimizer? | **Adam** (or **AdamW**) is the default for most tasks. |

</details>

---

## Exercise 11: "Learning Rate Schedules"

| Schedule | Description | When to Use |
|----------|-------------|-------------|
| Step Decay | | |
| Cosine Annealing | | |
| ReduceLROnPlateau | | |
| Linear Warm-up | | |

<details>
<summary>📖 Click for Answers</summary>

| Schedule | Description | When to Use |
|----------|-------------|-------------|
| **Step Decay** | Drop learning rate by a factor (e.g., 0.1) at specific epochs | When you know when to reduce (e.g., after 30, 60 epochs) |
| **Cosine Annealing** | Smoothly reduces LR from max to min following a cosine curve | When you want smooth, continuous decay; works well with SGD |
| **ReduceLROnPlateau** | Monitors validation loss; drops LR when metric stops improving | When you don't know when to reduce — automatic adaptation |
| **Linear Warm-up** | Gradually increases LR from small value to target over N iterations | At the start of training to prevent instability, especially with Adam |

</details>

---

## Exercise 12: "Batch Size Effects"

| Question | Your Answer |
|----------|-------------|
| 1. What are the pros of **small batches**? | |
| 2. What are the cons of **small batches**? | |
| 3. What are the pros of **large batches**? | |
| 4. What are the cons of **large batches**? | |
| 5. Why do large batches often generalize worse? | |
| 6. What is the Linear Scaling Rule? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. Pros of small batches | **Stochastic noise** helps escape sharp local minima; faster iterations; better generalization. |
| 2. Cons of small batches | Lower hardware utilization; noisier gradients require lower learning rates. |
| 3. Pros of large batches | Accurate gradient estimates; better parallelization (GPUs); higher throughput. |
| 4. Cons of large batches | Converge to **sharp minima** that generalize poorly; require careful LR scaling. |
| 5. Why do large batches generalize worse? | Large batches reduce the stochastic noise that helps the optimizer escape sharp minima and find flatter, more generalizable minima. |
| 6. Linear Scaling Rule | When increasing batch size by `k`, multiply learning rate by `k`. Keeps gradient variance consistent. |

</details>

---

## Exercise 13: "Second-Order Methods"

| Question | Your Answer |
|----------|-------------|
| 1. What is the Hessian matrix? | |
| 2. What is Newton's Method? | |
| 3. Why are second-order methods rarely used in deep learning? | |
| 4. What are Quasi-Newton methods (e.g., L-BFGS)? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is the Hessian matrix? | A square matrix of second-order partial derivatives: `H_ij = ∂²L/∂w_i∂w_j`. Captures curvature of the loss surface. |
| 2. What is Newton's Method? | Uses the Hessian to move directly toward the minimum of the local quadratic approximation: `w ← w - H⁻¹·∂L/∂w` (no learning rate needed). |
| 3. Why are second-order methods rarely used? | For a model with `N` parameters, the Hessian has `N²` elements. Storing and inverting it is infeasible for billions of parameters. |
| 4. What are Quasi-Newton methods? | Approximate the Hessian inverse without explicitly computing the full matrix (e.g., **L-BFGS**). More memory efficient but still limited to smaller models. |

</details>

---

## Exercise 14: "Optimization Geometry — Condition Number"

| Question | Your Answer |
|----------|-------------|
| 1. What is the condition number of the Hessian? | |
| 2. Why does a high condition number make optimization difficult? | |
| 3. How do adaptive optimizers like Adam help with ill-conditioned problems? | |
| 4. What does "preconditioning" mean? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is the condition number? | The ratio of the largest to smallest eigenvalue of the Hessian matrix. |
| 2. Why does high condition number make optimization difficult? | High condition number indicates "narrow valleys" — steep in one direction, flat in another. Gradient descent oscillates in steep directions and crawls in flat ones. |
| 3. How do adaptive optimizers help? | Adam performs approximate **preconditioning** — scales updates by the inverse of the moving average of squared gradients, making the surface appear more spherical. |
| 4. What does "preconditioning" mean? | A transformation that "stretches" flat directions and "compresses" steep ones, normalizing the geometry of the loss landscape for faster convergence. |

</details>

---

## Exercise 15: "Gradient Clipping"

| Question | Your Answer |
|----------|-------------|
| 1. What causes exploding gradients? | |
| 2. What is **value clipping**? | |
| 3. What is **norm clipping**? | |
| 4. Why is norm clipping preferred over value clipping? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What causes exploding gradients? | Exponential growth of gradients during the backward pass due to poor initialization, long sequences, or deep networks. Common in RNNs. |
| 2. What is value clipping? | Forces each element `g_i` into the range `[-c, c]`. Simple but may distort the gradient direction. |
| 3. What is norm clipping? | Rescales the entire gradient vector if its norm exceeds a threshold `T`: `g ← g · min(1, T/||g||)`. Preserves direction while capping magnitude. |
| 4. Why is norm clipping preferred? | Maintains the gradient **direction** while limiting magnitude — preserves the critical information from the gradient's orientation. |

</details>

---

## Exercise 16: "Normalization Techniques"

| Technique | Normalizes Across | Best For | Regularization Effect |
|-----------|-------------------|----------|----------------------|
| Batch Normalization (BN) | | | |
| Layer Normalization (LN) | | | |

<details>
<summary>📖 Click for Answers</summary>

| Technique | Normalizes Across | Best For | Regularization Effect |
|-----------|-------------------|----------|----------------------|
| **Batch Normalization (BN)** | **Batch dimension** (across samples) | CNNs, large batches | Injects stochastic noise via mini-batch statistics |
| **Layer Normalization (LN)** | **Feature dimension** (across neurons per sample) | RNNs, Transformers, variable-length sequences | Enforces structural stability; no batch dependence |

</details>

---

# SECTION 4: CORE TOPICS — REGULARIZATION

---

## Exercise 17: "L1 vs L2 Regularization"

| Aspect | L1 (Lasso) | L2 (Weight Decay) |
|--------|------------|-------------------|
| Penalty term | | |
| Effect on weights | | |
| Gradient update | | |
| Feature selection? | | |
| Geometric shape | | |

<details>
<summary>📖 Click for Answers</summary>

| Aspect | L1 (Lasso) | L2 (Weight Decay) |
|--------|------------|-------------------|
| Penalty term | `λ · Σ|w|` | `λ · Σw²` |
| Effect on weights | Pushes weights **exactly to zero** | Shrinks weights but rarely to zero |
| Gradient update | `w ← w - η·(∂L/∂w + λ·sign(w))` | `w ← w - η·(∂L/∂w + 2λ·w)` |
| Feature selection? | **Yes** — automatic feature selection | **No** — keeps all features but smooths them |
| Geometric shape | **Diamond** (L1 ball) | **Circle/Sphere** (L2 ball) |

</details>

---

## Exercise 18: "Dropout in Detail"

| Question | Your Answer |
|----------|-------------|
| 1. What does Dropout do during training? | |
| 2. What does Dropout do during inference? | |
| 3. Why do we scale activations at inference? | |
| 4. How does Dropout prevent co-adaptation? | |
| 5. What ensemble does Dropout approximate? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What does Dropout do during training? | Randomly sets a fraction `p` of neurons to 0 for each mini-batch. Prevents co-adaptation. |
| 2. What does Dropout do during inference? | All neurons are active. Output activations are scaled by `(1-p)` to compensate for training-time dropout. |
| 3. Why scale at inference? | During training, expected output = `(1-p)·activation`. Scaling at inference ensures consistent magnitude. |
| 4. How does Dropout prevent co-adaptation? | Forces each neuron to work independently — if one path is blocked, the model must find alternative representations. |
| 5. What ensemble does Dropout approximate? | Efficiently approximates training an ensemble of **2^N** different network architectures with shared weights. |

</details>

---

## Exercise 19: "Early Stopping"

| Question | Your Answer |
|----------|-------------|
| 1. What is Early Stopping? | |
| 2. What is the "patience" parameter? | |
| 3. Why does Early Stopping prevent overfitting? | |
| 4. Should you save the final weights or the best weights? | |
| 5. What's the trade-off between patience and performance? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is Early Stopping? | Monitoring validation loss during training and halting when it stops improving (even if training loss is still decreasing). |
| 2. What is the "patience" parameter? | Number of epochs to wait for an improvement before terminating. Prevents stopping due to random noise. |
| 3. Why does Early Stopping prevent overfitting? | Stops training at the "sweet spot" before the model begins to memorize training noise. |
| 4. Should you save final or best weights? | **Save the best weights** observed during training, not the final weights at termination. |
| 5. Trade-off between patience and performance? | Too little patience → stops prematurely (underfitting). Too much patience → may overfit. Typically patience = 5-20 epochs. |

</details>

---

## Exercise 20: "Data Augmentation"

| Question | Your Answer |
|----------|-------------|
| 1. What is Data Augmentation? | |
| 2. Name 3 augmentation techniques for **images**. | |
| 3. Name 2 augmentation techniques for **text**. | |
| 4. What is MixUp? | |
| 5. What is the regularization effect of MixUp? | |
| 6. What is a potential downside of aggressive augmentation? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is Data Augmentation? | Artificially expanding the training dataset by applying label-preserving transformations to input data. |
| 2. Image augmentation techniques | **Random cropping**, **Horizontal flipping**, **Color jittering**, **Rotation**, **CutOut** (randomly mask patches). |
| 3. Text augmentation techniques | **Back-translation** (translate to another language and back), **Synonym replacement**, **Random masking**. |
| 4. What is MixUp? | Creating new training examples by taking **linear combinations** of pairs of inputs and their labels: `x' = λx_i + (1-λ)x_j`, `y' = λy_i + (1-λ)y_j`, where `λ ~ Beta(α, α)`. |
| 5. Regularization effect of MixUp | Encourages **linear behavior between training examples**, smoothes decision boundaries, reduces overconfidence. |
| 6. Downside of aggressive augmentation | Can **corrupt the data** (e.g., extreme blurring/cropping makes labels invalid). Can slow down training due to preprocessing overhead. |

</details>

---

## Exercise 21: "Sharp vs Flat Minima"

| Question | Your Answer |
|----------|-------------|
| 1. What is a **sharp minimum**? | |
| 2. What is a **flat minimum**? | |
| 3. Which generalizes better? Why? | |
| 4. How does regularization promote flat minima? | |
| 5. Why do flat minima indicate a robust model? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is a sharp minimum? | Small shifts in parameter values or input data lead to large increases in loss. Indicates overfitting to training noise. |
| 2. What is a flat minimum? | The loss remains low across a wider range of parameter values. Indicates a robust, generalizable model. |
| 3. Which generalizes better? | **Flat minima** generalize better because they are less sensitive to perturbations and distribution shifts. |
| 4. How does regularization promote flatness? | L2 penalizes large weights; Dropout "blurs" the loss surface; both push the optimizer toward wider, flatter regions. |
| 5. Why do flat minima indicate robustness? | A flat minimum means the model is invariant to minor parameter changes — it's learned stable patterns, not fragile memorized details. |

</details>

---

## Exercise 22: "Ensemble Learning as Regularization"

| Question | Your Answer |
|----------|-------------|
| 1. What is Bagging? | |
| 2. What is Boosting? | |
| 3. How is Dropout like ensemble learning? | |
| 4. Why do ensembles generalize better? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What is Bagging? | Bootstrap Aggregating — trains multiple models on different subsets of the data. Aggregates predictions (e.g., averaging). Reduces variance without increasing bias. |
| 2. What is Boosting? | Sequentially trains models, where each subsequent model focuses on correcting the errors of the previous ones. Reduces bias and variance but more prone to overfitting. |
| 3. How is Dropout like ensemble? | Dropout approximates training an ensemble of **2^N** different subnetworks with shared weights, averaged at inference. |
| 4. Why do ensembles generalize better? | By aggregating independent or complementary predictions, ensembles mitigate the risk of relying on a single, potentially overfitted hypothesis. |

</details>

---

# SECTION 5: DEEP APPLICATION

---

## Exercise 23: "The Training Pipeline Design"

**Scenario:** You're training a deep CNN on 100,000 images (10 classes). You observe:

- Training accuracy: 99%
- Validation accuracy: 78%

| Question | Your Answer |
|----------|-------------|
| 1. What problem does this indicate? | |
| 2. Name 3 regularization techniques to apply and explain why each helps. | |
| 3. Which optimizer would you start with? Why? | |
| 4. What learning rate schedule would you recommend? | |
| 5. How would you monitor for overfitting? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. What problem does this indicate? | **Severe overfitting** — the model is memorizing the training data (99%) and failing to generalize (78%). |
| 2. Name 3 regularization techniques | **Dropout** (prevents co-adaptation of neurons), **Data Augmentation** (expands training variety), **L2 Weight Decay** (penalizes large weights), **Early Stopping** (monitor validation loss). |
| 3. Which optimizer? | **Adam** (or AdamW) — robust default with adaptive learning rates, handles noisy gradients well. |
| 4. What learning rate schedule? | **Cosine Annealing** or **ReduceLROnPlateau** — adaptive schedule that automatically reduces LR when validation loss plateaus. |
| 5. How to monitor overfitting? | Track **training vs validation loss** and **training vs validation accuracy**. If the gap widens, you're overfitting. |

</details>

---

## Exercise 24: "Batch Normalization: Friend or Foe?"

**Scenario:** You're training a Transformer for language modeling. You apply Batch Normalization but get poor results.

| Question | Your Answer |
|----------|-------------|
| 1. Why might BN be problematic for Transformers? | |
| 2. What normalization should you use instead? | |
| 3. What is the key difference between BN and LN? | |
| 4. Does LN have a regularization effect? | |

<details>
<summary>📖 Click for Answers</summary>

| Question | Answer |
|----------|--------|
| 1. Why might BN be problematic for Transformers? | BN depends on batch size and sequence length. Transformers process variable-length sequences; BN statistics become unstable. |
| 2. What normalization should you use instead? | **Layer Normalization (LN)** — normalizes across features per sample, independent of batch size and sequence length. |
| 3. Key difference between BN and LN | BN normalizes across the **batch dimension** (samples). LN normalizes across the **feature dimension** (neurons per sample). |
| 4. Does LN have a regularization effect? | Yes — enforces consistent mean/variance across hidden units, providing structural stability and acting as an implicit regularizer. |

</details>

---

## Exercise 25: "Gradient Diagnostic Checklist"

For each symptom, diagnose the likely issue and suggest a fix:

| Symptom | Diagnosis | Fix |
|---------|-----------|-----|
| Gradients are near 0 in early layers | | |
| Gradients are NaN in early layers | | |
| Loss is flat (not decreasing) after many epochs | | |
| Training loss decreases, validation loss increases | | |
| Training loss is high, validation loss is high and similar | | |
| Many ReLU neurons output 0 consistently | | |

<details>
<summary>📖 Click for Answers</summary>

| Symptom | Diagnosis | Fix |
|---------|-----------|-----|
| Gradients near 0 in early layers | **Vanishing Gradients** | Use ReLU/GELU, Batch Norm, Residual Connections |
| Gradients are NaN | **Exploding Gradients** | Lower LR, Gradient Clipping, better weight initialization |
| Loss is flat (not decreasing) | **Saturation / Dead Neurons** | Check activation functions, switch from Sigmoid/Tanh to ReLU; adjust LR |
| Train loss decreases, Val loss increases | **Overfitting** | Add Dropout, L2 regularization, Data Augmentation, Early Stopping |
| Train loss high, Val loss high and similar | **Underfitting** | Increase model complexity, add layers/neurons, train longer, reduce regularization |
| Many ReLU neurons output 0 | **Dying ReLU** | Use Leaky ReLU, ELU, or He initialization |

</details>

---

# SECTION 6: MASTERY & SYNTHESIS

---

## Exercise 26: "The Complete Update Flow"

Place the following operations in the correct order during backpropagation:

| Step | Description | Order (1-6) |
|------|-------------|-------------|
| A | Update weights `w ← w - η·∂L/∂w` | |
| B | Compute output gradient `δ^(L) = ∂L/∂a^(L) ⊙ f'(z^(L))` | |
| C | Compute `δ^(l)` for each layer using `δ^(l) = (W^(l+1))ᵀ · δ^(l+1) ⊙ f'(z^(l))` | |
| D | Compute gradient for weights `∂L/∂w^(l) = δ^(l) · (a^(l-1))ᵀ` | |
| E | Forward pass: compute `z^(l)` and `a^(l)` for all layers | |
| F | Compute loss `L` from prediction and target | |

<details>
<summary>📖 Click for Answers</summary>

| Step | Description | Order |
|------|-------------|-------|
| E | Forward pass: compute `z^(l)` and `a^(l)` for all layers | **1** |
| F | Compute loss `L` from prediction and target | **2** |
| B | Compute output gradient `δ^(L)` | **3** |
| C | Backpropagate errors through layers | **4** |
| D | Compute gradient for weights | **5** |
| A | Update weights | **6** |

</details>

---

## Exercise 27: "Regularization Strategy Selection"

For each scenario, recommend the best regularization technique and explain why:

| Scenario | Best Technique(s) | Why? |
|----------|-------------------|------|
| You have a tiny dataset (100 samples) | | |
| You have a massive dataset (10M samples) | | |
| Your model is a Transformer for NLP | | |
| Your model is a deep CNN for images | | |
| Your model is an RNN for time-series | | |
| You need sparse weights for interpretability | | |

<details>
<summary>📖 Click for Answers</summary>

| Scenario | Best Technique(s) | Why? |
|----------|-------------------|------|
| Tiny dataset (100 samples) | **Heavy Data Augmentation + Dropout + Early Stopping** | Limited data = high risk of memorization. Aggressive regularization needed. |
| Massive dataset (10M samples) | **Light regularization (small weight decay)** | Large data reduces overfitting risk; heavy regularization may underfit. |
| Transformer for NLP | **Dropout + Layer Normalization + Weight Decay (AdamW)** | LN is preferred for sequences; Dropout and weight decay are standard. |
| Deep CNN for images | **Data Augmentation + Dropout + Batch Normalization** | BN works well with images; augmentation is essential for vision. |
| RNN for time-series | **Dropout + Layer Normalization + Gradient Clipping** | LN handles variable-length sequences; gradient clipping prevents exploding gradients in RNNs. |
| Sparse weights for interpretability | **L1 Regularization (Lasso)** | L1 pushes weights exactly to zero, performing automatic feature selection. |

</details>

---

## Exercise 28: "The Complete Cheat Sheet"

### Fill in all blanks:

**Definition of Backpropagation:**
Backpropagation efficiently computes the gradient of the ___________ with respect to every ___________ in the network using the ___________.

**Backpropagation Lifecycle:**

| Phase | What happens |
|-------|--------------|
| Phase 1: ___________ | Compute activations and store intermediates |
| Phase 2: ___________ | Traverse graph in reverse; apply ___________ |
| Phase 3: ___________ | Use gradients to update weights |

**Optimizer Comparison:**

| Optimizer | Core Idea | Best For |
|-----------|-----------|----------|
| SGD | | |
| Momentum | | |
| RMSProp | | |
| Adam | | |

**Regularization Techniques Summary:**

| Technique | Mechanism | Best Used For |
|-----------|-----------|---------------|
| L2 (Weight Decay) | | |
| L1 (Lasso) | | |
| Dropout | | |
| Batch Normalization | | |
| Layer Normalization | | |
| Early Stopping | | |
| Data Augmentation | | |
| MixUp | | |

**Bias-Variance Tradeoff with Regularization:**
- Regularization increases ___________ and decreases ___________
- The goal is to find the ___________ that minimizes total error

**Gradient Flow Diagnostics:**

| Problem | Symptoms | Fixes |
|---------|----------|-------|
| Vanishing Gradients | | |
| Exploding Gradients | | |
| Dying ReLU | | |

<details>
<summary>📖 Click for Answers</summary>

**Definition of Backpropagation:**
Backpropagation efficiently computes the gradient of the **loss function** with respect to every **weight** in the network using the **chain rule**.

**Backpropagation Lifecycle:**

| Phase | What happens |
|-------|--------------|
| Phase 1: **Forward Pass** | Compute activations and store intermediates |
| Phase 2: **Backward Pass** | Traverse graph in reverse; apply **chain rule** |
| Phase 3: **Weight Update** | Use gradients to update weights |

**Optimizer Comparison:**

| Optimizer | Core Idea | Best For |
|-----------|-----------|----------|
| **SGD** | Raw gradient descent | Simple problems, small models |
| **Momentum** | Moving average of gradients | Dampening oscillations |
| **RMSProp** | Adaptive per-parameter LRs | Uneven gradient magnitudes |
| **Adam** | Momentum + RMSProp | Most modern tasks (default) |

**Regularization Techniques Summary:**

| Technique | Mechanism | Best Used For |
|-----------|-----------|---------------|
| **L2 (Weight Decay)** | Penalizes squared weights | Most models (standard) |
| **L1 (Lasso)** | Penalizes absolute weights | Feature selection / sparsity |
| **Dropout** | Randomly drops neurons | Preventing co-adaptation |
| **Batch Normalization** | Normalizes across batch | CNNs, large batches |
| **Layer Normalization** | Normalizes across features | Transformers, RNNs, sequences |
| **Early Stopping** | Halts when val loss stalls | All models (prevent over-training) |
| **Data Augmentation** | Creates synthetic data | Vision tasks (images) |
| **MixUp** | Linear combinations of samples | Smoother decision boundaries |

**Bias-Variance Tradeoff with Regularization:**
- Regularization increases **bias** and decreases **variance**
- The goal is to find the **sweet spot (optimal complexity)** that minimizes total error

**Gradient Flow Diagnostics:**

| Problem | Symptoms | Fixes |
|---------|----------|-------|
| Vanishing Gradients | Gradients near 0 in early layers | ReLU/GELU, Batch Norm, Residual Connections |
| Exploding Gradients | Gradients become NaN | Gradient Clipping, Lower LR, Better Initialization |
| Dying ReLU | Many ReLU neurons output 0 | Leaky ReLU, ELU, He Initialization |

</details>

---

# 📊 COMPLETE COVERAGE SUMMARY (WEEK 2)

| Category | Concepts | Status |
|----------|----------|--------|
| Backpropagation (L1) | Chain rule, computational graphs, local gradients | ✅ 100% |
| Backpropagation (L1) | Jacobian matrices, automatic differentiation | ✅ 100% |
| Backpropagation (L1) | Update formulas (MSE, BCE), numerical precision | ✅ 100% |
| Backpropagation (L1) | Branching paths, gradient aggregation, scaling | ✅ 100% |
| Optimization (L2) | SGD, Momentum, RMSProp, Adam | ✅ 100% |
| Optimization (L2) | Learning rate schedules, warm-up, batch size effects | ✅ 100% |
| Optimization (L2) | Second-order methods, curvature, preconditioning | ✅ 100% |
| Optimization (L2) | Gradient clipping, distributed optimization | ✅ 100% |
| Regularization (L3) | Dropout, L1/L2, Early Stopping | ✅ 100% |
| Regularization (L3) | BN, LN, Data Augmentation, MixUp | ✅ 100% |
| Regularization (L3) | Flat vs Sharp minima, ensembles | ✅ 100% |
| **TOTAL** | **80+ concepts** | ✅ **100%** |